export interface responseTransfer {
    readonly amount: number;
    readonly nameSender: string;
    readonly nameAddressee: string;
    readonly email: string;
    readonly destinationBank: string;
}